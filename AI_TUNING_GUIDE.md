# AI聊天分析标准调教指南

## 📍 核心文件位置

**主要文件**：`backend/src/common/services/ai/deepseek-analysis.service.ts`

这个文件包含所有AI分析的提示词（prompt）和评估标准。

---

## 🎯 可调整的AI标准

### 1. 质量等级评定标准 (A/B/C/D)

**当前标准**（第188行）：
```typescript
"qualityLevel": "A/B/C/D（A=高价值，B=中等价值，C=一般，D=低价值）"
```

**如何调整**：
编辑第186-234行的 `getJsonTemplate()` 方法，详细说明每个等级的判定标准。

**示例修改**：
```typescript
private getJsonTemplate(): string {
  return `{
  "qualityLevel": "A/B/C/D（
    A=高价值：明确报名意向+预算充足+决策周期短（1周内）
    B=中等价值：有报名意向+预算待确认+1-4周决策
    C=一般：意向不明确或观望中+价格敏感
    D=低价值：无明确意向或严重价格敏感或竞品倾向强
  ）",
  "riskLevel": "无风险/低/中/高（
    高风险：提到竞品+严重投诉+退费倾向
    中风险：犹豫不决+价格异议+负面情绪
    低风险：轻微顾虑
    无风险：态度积极无异议
  ）",
  "intentionScore": 85,  // 0-100分的评分标准：
                         // 90-100：强烈报名意向，主动询问细节
                         // 70-89：有兴趣，但有顾虑需解决
                         // 40-69：观望态度，需持续跟进
                         // 0-39：意向弱，不建议投入过多精力
  ...
```

### 2. 意向分数转化标准

**当前标准**（`ai-chat.service.ts` 第175-194行）：
```typescript
let newIntent = '中';
if (qualityLevel === 'A' || intentionScore >= 80) {
  newIntent = '高';
} else if (qualityLevel === 'D' || intentionScore < 40) {
  newIntent = '低';
}
```

**如何调整**：
根据您的业务实际情况修改分数阈值。

**示例修改**：
```typescript
let newIntent = '中';
// 更严格的标准：只有90分以上才算高意向
if (qualityLevel === 'A' && intentionScore >= 90) {
  newIntent = '高';
}
// 或放宽标准：70分就算高意向
else if (qualityLevel === 'A' || intentionScore >= 70) {
  newIntent = '高';
}
else if (qualityLevel === 'D' || intentionScore < 30) {
  newIntent = '低';
}
```

### 3. 系统提示词（AI角色定义）

**当前提示词**（第144-159行）：
```typescript
private getSystemPrompt(): string {
  return `你是一个专业的教育培训行业销售分析专家，擅长分析微信聊天记录并提供深度洞察。

你的任务是：
1. 深入分析销售与客户的微信聊天记录
2. 从20+个维度全面评估客户情况
3. 识别客户需求、痛点、异议和风险
4. 评估商机价值和成交可能性
5. 提供具体可执行的销售策略和话术建议

分析要求：
- 客观准确，基于事实证据
- 洞察深入，发现隐藏信息
- 建议具体，可直接执行
- 输出格式为严格的JSON，方便系统解析`;
}
```

**如何调整**：
根据您的具体业务（比如K12/成人培训/艺术培训等）调整专业领域。

**示例修改**（针对K12一对一辅导）：
```typescript
private getSystemPrompt(): string {
  return `你是一个专业的K12一对一辅导销售分析专家，擅长分析家长与销售的微信聊天记录。

你的专业背景：
- 深入理解K12家长的决策心理
- 熟悉不同年级学生的学习需求
- 了解家长的顾虑点和敏感话题
- 掌握竞品动态（学而思、新东方等）

你的任务是：
1. 分析家长的真实需求和顾虑
2. 评估孩子的学习情况和紧迫性
3. 判断家长的决策能力和预算水平
4. 识别成交障碍和竞争威胁
5. 提供针对性的跟进策略

分析标准：
- 重点关注：成绩提升需求、升学压力、家长焦虑度
- 风险识别：价格敏感、竞品对比、效果怀疑
- 成交预判：基于预算、时间点（期中考/期末考前）、决策角色
- 输出格式为严格的JSON`;
}
```

### 4. 商机价值评估标准

**当前模板**（第216-220行）：
```typescript
"estimatedValue": 5000,  // 预估成交金额
"estimatedCycle": "短期（1周内）/中期（1-4周）/长期（1个月+）",
"dealOpportunity": "高/中/低",
"urgency": "高/中/低",
"competitiveness": "高/中/低",
```

**如何调整**：
在JSON模板中添加更详细的评估标准说明。

**示例修改**：
```typescript
"estimatedValue": 5000,  // 预估金额计算：
                         // - 提到"一对一"：6000-10000元/科
                         // - 提到"小班课"：3000-5000元/科
                         // - 提到"试听"暂无成交：500元

"estimatedCycle": "短期/中期/长期（
  短期：明确表示本周内决定 或 考试前急需
  中期：需要考虑一下/和家人商量
  长期：先试听看看/暂时不着急
）",

"urgency": "高/中/低（
  高：期末考前/升学关键期/成绩严重下滑
  中：有提升需求但无明确时间点
  低：随便看看/了解一下
）",
```

### 5. 风险预警标准

**当前模板**（第231-232行）：
```typescript
"riskFactors": ["风险因素1"],
"riskReason": "风险原因详细说明"
```

**如何调整**：
在系统提示词中明确定义哪些属于高风险信号。

**示例修改**（在系统提示词末尾添加）：
```typescript
private getSystemPrompt(): string {
  return `你是一个专业的教育培训行业销售分析专家...

高风险信号识别标准：
1. 客户提到其他机构名称（学而思、新东方等）
2. 明确表示价格太贵/超预算
3. 对教学效果表示怀疑
4. 沟通态度从积极转为冷淡
5. 多次爽约/不回复消息
6. 提到退费、投诉等负面词汇
7. 家长内部意见不统一

输出格式为严格的JSON，方便系统解析`;
}
```

---

## 📝 常见调整场景

### 场景1：AI评分太严格，很少给A级

**问题**：大部分客户都被评为B/C级，销售觉得不准确。

**解决**：放宽A级标准

```typescript
// 在 getJsonTemplate() 中修改
"qualityLevel": "A/B/C/D（
  A=高价值：有报名意向 + 预算5000以上 OR 明确要求试听
  B=中等价值：有兴趣但顾虑较多
  C=一般：观望态度
  D=低价值：无意向或严重价格敏感
）",
```

### 场景2：AI评分太宽松，给了很多假A级

**问题**：很多观望客户被评为A级，跟进后发现不靠谱。

**解决**：提高A级门槛

```typescript
"qualityLevel": "A/B/C/D（
  A=高价值：明确表示要报名 + 已询问具体班次/老师 + 预算充足
  B=中等价值：有意向但需考虑
  C=一般：随便问问
  D=低价值：无意向
）",
```

### 场景3：需要更细致的预算判断

**问题**：AI无法准确判断客户预算。

**解决**：在提示词中添加预算判断规则

```typescript
// 在系统提示词中添加
家庭经济水平判断标准：
- 高：主动询问VIP课程/不问价格/职业为高管、医生、律师
- 中：关心性价比/对比多家机构/普通工薪家庭
- 低：多次强调价格/要求优惠/明确表示预算有限
```

### 场景4：希望AI给出更具体的行动建议

**问题**：nextSteps太泛泛，不够具体。

**解决**：在提示词中要求更详细的建议

```typescript
// 修改系统提示词
"nextSteps": [
  "必须包含具体时间：如'今天下午3点前发送试听邀请'",
  "必须包含具体话术：如'发送消息：张女士，针对您关心的xxx问题...'",
  "必须包含可验证结果：如'确认客户周六上午是否有空参加试听'"
],
```

---

## 🔧 实际操作步骤

### 方法A：直接修改代码文件（推荐）

1. **编辑文件**：
```bash
# 本地编辑
code D:\CC\1.1\backend\src\common\services\ai\deepseek-analysis.service.ts
```

2. **修改对应方法**：
   - `getSystemPrompt()` - 修改AI角色和基本要求
   - `getJsonTemplate()` - 修改评分标准说明

3. **提交并推送**：
```bash
cd D:\CC\1.1
git add backend/src/common/services/ai/deepseek-analysis.service.ts
git commit -m "调整AI分析评分标准"
git push origin master
```

4. **服务器同步**：
```bash
ssh root@106.53.77.212
cd /root/crm
git pull origin master
docker-compose stop backend
docker-compose build backend
docker-compose up -d backend
```

### 方法B：通过配置文件（可选，需要代码改造）

如果您希望不改代码就能调整标准，我可以帮您改造成配置文件方式：

```bash
# 创建 backend/prompts/analysis-standard.json
{
  "qualityLevel": {
    "A": "明确报名意向 + 预算充足 + 1周内决策",
    "B": "有意向 + 需考虑 + 1-4周",
    "C": "观望态度",
    "D": "无意向或严重价格敏感"
  },
  "intentionScore": {
    "high": 80,
    "low": 40
  },
  ...
}
```

然后代码从配置文件读取标准。**需要我帮您改造吗？**

---

## 🧪 测试调整效果

修改后，用相同的聊天记录重新上传测试：

1. **删除缓存**（如果有）：
```bash
# SSH到服务器
docker exec -it crm-redis redis-cli
FLUSHDB  # 清空AI分析缓存
exit
```

2. **重新上传相同截图**，对比分析结果

3. **检查日志**：
```bash
docker-compose logs -f backend | grep "AI分析"
```

---

## 💡 调教建议

### 1. 渐进式调整
不要一次性大改，每次调整一个标准，测试效果后再继续。

### 2. 基于真实数据
用最近3-5个真实成交案例的聊天记录测试，看AI评分是否准确。

### 3. 团队共识
和销售团队讨论，确保AI的评分标准和销售的实际判断一致。

### 4. 定期优化
每月回顾AI评分准确率，根据成交情况调整标准。

---

## ❓ 您想调整哪些标准？

告诉我您的具体需求，比如：
1. "A级客户定义太宽松，需要收紧"
2. "预算评估不准，经常高估"
3. "风险预警太敏感，小问题也报警"
4. "行动建议太泛泛，不够具体"

我帮您直接修改代码！
