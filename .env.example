# ========================================
# 海绵青年CRM系统 - 生产环境配置模板
# ========================================
# 复制此文件为 .env 并填写实际配置值
# 命令: cp .env.example .env

# ========================================
# 数据库配置
# ========================================
DB_HOST=mysql
DB_PORT=3306
DB_USERNAME=root
DB_PASSWORD=your_secure_password_here
DB_DATABASE=education_crm

# ========================================
# JWT认证配置
# ========================================
# 建议使用随机字符串，可以通过以下命令生成：
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_jwt_secret_key_here
JWT_EXPIRES_IN=7d

# ========================================
# 应用端口配置（可选）
# ========================================
# 如果需要修改默认端口，取消注释并修改以下配置
# FRONTEND_PORT=80
# BACKEND_PORT=3000
# MYSQL_PORT=3306

# ========================================
# 其他配置
# ========================================
# Node环境
NODE_ENV=production

# 时区
TZ=Asia/Shanghai
